# Visual Question Answering

Ask natural language questions about images and get detailed answers with Moondream.

## Installation & Model

```bash filename="terminal"
pip install moondream==0.0.4
```

Download the model from our [Model Downloads](/model-downloads) page.

## Basic Usage

```python filename="example.py"
from moondream import vl
model = vl("models/moondream-2b-int8.bin.gz")

# Ask a simple question
answer = model.query(image, "What is the main color in this image?")
print(answer['answer'])

# Ask about objects
answer = model.query(image, "How many people are in this image?")
print(answer['answer'])
```

That's all you need to start asking questions about images!

---

## Next Steps

- Check out our [Examples](/cookbook/basic-usage) to see question answering in action with sample code and use cases
- Learn about model specifications and performance in our [Technical Specifications](/technical-specifications) guide
- Want to use moondream with Transformers? Check out our [Integration Guide](/advanced/transformers)