# Object Detection 🚧

import { Callout } from 'nextra/components'
import { Steps } from 'nextra/components'
import { Tabs } from 'nextra/components'

<Callout type="info">
  Detect and locate objects in images with high precision using Moondream's object detection capabilities.
</Callout>

## Overview

Moondream's object detection feature offers:
- Precise object localization
- Confidence scores for detections
- Support for multiple object classes
- Bounding box coordinates

## Interactive Demo

<iframe 
  src="https://moondream.ai/playground?feature=detect" 
  width="100%" 
  height="600px" 
  frameBorder="0"
  style={{ borderRadius: '8px', boxShadow: '0 4px 6px -1px rgb(0 0 0 / 0.1)' }}
/>

## Implementation Guide

<Steps>
### 1. Initialize the API Client
<Tabs items={['Python', 'JavaScript', 'TypeScript']}>
  <Tabs.Tab>
    ```python
    from moondream import MoondreamAPI
    
    api = MoondreamAPI()
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```javascript
    import { MoondreamAPI } from 'moondream';
    
    const api = new MoondreamAPI();
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    import { MoondreamAPI } from 'moondream';
    
    const api = new MoondreamAPI();
    ```
  </Tabs.Tab>
</Tabs>

### 2. Prepare Your Request
<Tabs items={['Python', 'JavaScript', 'TypeScript']}>
  <Tabs.Tab>
    ```python
    request = DetectRequest(
        image_url="https://example.com/image.jpg",
        object="person"  # Object class to detect
    )
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```javascript
    const request = {
      imageUrl: "https://example.com/image.jpg",
      object: "person"  // Object class to detect
    };
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    const request: DetectRequest = {
      imageUrl: "https://example.com/image.jpg",
      object: "person"  // Object class to detect
    };
    ```
  </Tabs.Tab>
</Tabs>

### 3. Detect Objects
<Tabs items={['Python', 'JavaScript', 'TypeScript']}>
  <Tabs.Tab>
    ```python
    # Detect objects
    response = await api.detect_object(
        image_url="https://example.com/image.jpg",
        object="person"
    )
    
    # Process results
    for detection in response.result:
        confidence = detection['confidence']
        bbox = detection['bbox']  # [x1, y1, x2, y2]
        print(f"Found object with {confidence:.2f} confidence at {bbox}")
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```javascript
    // Detect objects
    const response = await api.detectObject({
      imageUrl: "https://example.com/image.jpg",
      object: "person"
    });
    
    // Process results
    response.result.forEach(detection => {
      const { confidence, bbox } = detection;
      console.log(`Found object with ${confidence.toFixed(2)} confidence at ${bbox}`);
    });
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    interface Detection {
      confidence: number;
      bbox: [number, number, number, number];
    }

    // Detect objects
    const response = await api.detectObject({
      imageUrl: "https://example.com/image.jpg",
      object: "person"
    });
    
    // Process results
    response.result.forEach((detection: Detection) => {
      const { confidence, bbox } = detection;
      console.log(`Found object with ${confidence.toFixed(2)} confidence at ${bbox}`);
    });
    ```
  </Tabs.Tab>
</Tabs>
</Steps>

## Best Practices

<Callout type="warning">
  For best results, ensure images are clear and well-lit. Consider using confidence thresholds to filter detections.
</Callout>

- Set appropriate confidence thresholds
- Process high-resolution images
- Consider object size and visibility
- Handle multiple detections appropriately

## Example Use Cases

1. **Security & Surveillance**
   - Person detection
   - Object tracking
   - Intrusion detection

2. **Retail Analytics**
   - Product detection
   - Shelf monitoring
   - Customer behavior analysis

3. **Smart Cities**
   - Vehicle detection
   - Pedestrian counting
   - Traffic monitoring

## Error Handling

<Tabs items={['Python', 'JavaScript', 'TypeScript']}>
  <Tabs.Tab>
    ```python
    try:
        response = await api.detect_object(
            image_url="https://example.com/image.jpg",
            object="person"
        )
    except InvalidImageError:
        print("Invalid image URL or format")
    except APIError as e:
        print(f"API error: {str(e)}")
    except Exception as e:
        print(f"Unexpected error: {str(e)}")
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```javascript
    try {
      const response = await api.detectObject({
        imageUrl: "https://example.com/image.jpg",
        object: "person"
      });
    } catch (error) {
      if (error instanceof InvalidImageError) {
        console.error("Invalid image URL or format");
      } else if (error instanceof APIError) {
        console.error(`API error: ${error.message}`);
      } else {
        console.error(`Unexpected error: ${error.message}`);
      }
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```typescript
    try {
      const response = await api.detectObject({
        imageUrl: "https://example.com/image.jpg",
        object: "person"
      });
    } catch (error) {
      if (error instanceof InvalidImageError) {
        console.error("Invalid image URL or format");
      } else if (error instanceof APIError) {
        console.error(`API error: ${error.message}`);
      } else {
        console.error(`Unexpected error: ${error.message}`);
      }
    }
    ```
  </Tabs.Tab>
</Tabs>

## Related Resources

- [Getting Started Guide](/docs/getting-started)
- [API Reference](/docs/api-reference)
- [Example Projects](/docs/examples) 