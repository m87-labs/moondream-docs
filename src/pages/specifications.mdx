import Head from "next/head";
import { Tabs } from "nextra/components";
import { Callout } from "nextra/components";
import FeatureSupport from "../components/FeatureSupport";
import Benchmarks from "../components/Benchmarks";
import ModelVariants from "../components/ModelVariants";

<Head>
  <title>Specifications - Moondream Documentation</title>
  <meta
    name="description"
    content="Detailed technical specifications for Moondream vision AI, including model downloads, memory requirements, performance benchmarks, and hardware recommendations."
  />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

{/* Open Graph */}

{" "}

<meta
  property="og:title"
  content="Moondream Technical Specifications - Model Details & Performance"
/>
<meta
  property="og:description"
  content="Detailed technical specifications for Moondream vision AI, including model architecture, memory requirements, performance benchmarks, and hardware recommendations."
/>
<meta property="og:type" content="website" />
<meta property="og:url" content="https://docs.moondream.ai/specifications" />

{/* Twitter */}

{" "}

<meta name="twitter:card" content="summary_large_image" />
<meta
  name="twitter:title"
  content="Moondream Technical Specifications - Model Details & Performance"
/>
<meta
  name="twitter:description"
  content="Detailed technical specifications for Moondream vision AI, including model architecture, memory requirements, performance benchmarks, and hardware recommendations."
/>

{/* Schema.org for Google */}

{" "}

<script type="application/ld+json">
  {JSON.stringify({
    "@context": "https://schema.org",
    "@type": "TechArticle",
    headline: "Moondream Vision AI Technical Specifications",
    description:
      "Comprehensive technical details and performance metrics for Moondream",
    articleSection: "Technical Documentation",
    keywords:
      "model architecture, memory requirements, performance benchmarks, hardware specifications, technical details",
  })}
</script>

</Head>

# Technical Specifications

<div className="text-lg text-gray-600 dark:text-gray-300 mb-12">
  Moondream offers a range of models optimized for different use cases, from
  edge devices to high-performance servers. All models support the same core
  capabilities with different performance characteristics.
</div>

## Model Variants

<ModelVariants />

## Benchmarks

<Benchmarks />

## Feature Support

<div className="mt-4 mb-8">
  <FeatureSupport />
  
  <div className="mt-6 space-y-4">
    <Callout type="info">
      The experimental features are available but may have limitations or
      require additional configuration.
    </Callout>

    <Callout type="note">
      Features marked as "coming soon" are under active development and will be
      available in future releases.
    </Callout>

  </div>
</div>

export const calculateDiff = (smaller, larger) => {
  const percentDiff = (((larger - smaller) / smaller) * 100).toFixed(0);
  return {
    percent: percentDiff,
    reduction: (((larger - smaller) / larger) * 100).toFixed(0),
  };
};

export const formatComparison = (metrics05b, metrics2b) => {
  const speedRatio = (metrics05b.speed / metrics2b.speed).toFixed(1);
  const ramDiff = calculateDiff(metrics05b.ram, metrics2b.ram);

return {
speed: {
text: `${speedRatio}x faster`,
comparison: `${metrics05b.speed} vs ${metrics2b.speed} tokens/s`,
},
ram: {
text: `${ramDiff.reduction}% less RAM`,
comparison: `${metrics05b.ram.toLocaleString()} vs ${metrics2b.ram.toLocaleString()} MB`,
},
};
};

export const metrics = {
  raspberryPi5: {
    "0.5b": { speed: 0.7, ram: 1534, latency: 1.44 },
    "2b": { speed: 0.19, ram: 3564, latency: 1.44 },
  },
  orangePi5: {
    "0.5b": { speed: 1.35, ram: 1497, latency: 1.34 },
  },
};
